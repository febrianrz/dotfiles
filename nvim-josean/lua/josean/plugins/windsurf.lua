return {
  {
    "Exafunction/windsurf.vim",
    event = "BufEnter",
    config = function()
      -- Plugin configuration options bisa ditambahkan di sini jika diperlukan
      -- Contoh:
      -- vim.g.windsurf_options = {
      --   some_option = true,
      --   another_option = "value"
      -- }
    end,
  },
}
